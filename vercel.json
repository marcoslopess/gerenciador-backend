{
  "version": 2,
  "builds": [
    {
      "src": "nest build && prisma generate",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/.*",
      "dest": "/dist/main.js"
    }
  ]
}
